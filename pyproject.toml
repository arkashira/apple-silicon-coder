[project]
name = "apple-silicon-coder"
version = "0.1.0"
description = "Local REST API for model"
requires-python = ">=3.8"
dependencies = []

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra -q"
testpaths = ["tests"]
